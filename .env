XAUTHORITY=/tmp/.docker.xauth
DISPLAY=${DISPLAY}

UID=1000 #$(id -u)
GID=1000 #$(id -g)

# For Fedora
QT_QPA_PLATFORM=wayland
WAYLAND_DISPLAY=wayland-0
XDG_RUNTIME_DIR=/run/user/1000  # /run/user/$(id -u)
